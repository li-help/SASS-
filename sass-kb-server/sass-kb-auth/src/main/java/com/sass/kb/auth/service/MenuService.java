package com.sass.kb.auth.service;

import org.springframework.stereotype.Service;

@Service
public class MenuService {

    public void initDefaultMenus(String tenantId) {
        // Menus are now seeded via Flyway V12 migration
    }
}

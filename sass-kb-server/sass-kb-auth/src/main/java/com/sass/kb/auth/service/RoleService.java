package com.sass.kb.auth.service;

import org.springframework.stereotype.Service;

@Service
public class RoleService {

    public void initDefaultRoles(String tenantId) {
        // Default roles are created via database seed
    }
}
